import { Token } from './types';
export declare const SYMBOL_LOOKUP: {
    [key in number]: {
        [key in string]: Token;
    };
};
export declare const ADDRESS_LOOKUP: {
    [key in number]: {
        [key in string]: Token;
    };
};
